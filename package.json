{
    "name": "11bugs",
    "version": "0.0.0",
    "description": "Personal Project",
    "author": "Raghav Agrawal",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "NODE_ENV=production node ./server/server.ts",
        "dev": "NODE_ENV=development && ts-node-dev --project tsconfig.json --respawn --inspect --transpile-only ./server/server.ts",
        "build": "cd client && npm install && npm run build"
    },
    "dependencies": {
        "axios": "^0.26.1",
        "bcrypt": "^5.0.1",
        "cheerio": "^1.0.0-rc.10",
        "cookie": "^0.4.2",
        "cookie-parser": "^1.4.6",
        "cors": "^2.8.5",
        "dotenv": "^16.0.0",
        "express": "^4.17.3",
        "jsonwebtoken": "^8.5.1",
        "mongoose": "^6.2.8",
        "pretty": "^2.0.0"
    },
    "devDependencies": {
        "@types/node": "^20.4.10",
        "typescript": "^5.1.6",
        "@types/express": "^4.17.14",
        "ts-node-dev": "^2.0.0"
    }
}
