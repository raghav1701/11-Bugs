import { Login } from "@/components/pages/login";

import { TNextPageWithLayout } from "@/types";

import { ReactElement } from "react";

const LoginScreen: TNextPageWithLayout = (): ReactElement => {
    return <Login />;
};

export default LoginScreen;
